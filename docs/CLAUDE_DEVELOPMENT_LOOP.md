# Claude Development Loop

This document is retained only as archived tool-specific history.

It is no longer a core process authority.
The active implementation process is now agent-neutral and lives in:
- `IMPLEMENTATION_WORKFLOW.md`
- `git_workflow_cheatsheet.md`
- the core authority docs (`00`–`05`)

If any guidance here conflicts with the current package, the current package wins.
